// =============================================================================
// FORGE — React Native port target
// =============================================================================
//
// This file is empty. The web version lives in WorkoutTracker.web.jsx.
//
// To port: copy the web file's logic in here and replace web-only constructs
// with React Native equivalents. See README.md for the full porting checklist.
//
// When done, update App.js to import this file:
//   import WorkoutTracker from './src/WorkoutTracker';
//   export default WorkoutTracker;
// =============================================================================

import React from 'react';
import { View, Text } from 'react-native';

export default function WorkoutTracker() {
  return (
    <View>
      <Text>Forge — to be ported</Text>
    </View>
  );
}
